# 阿里云 8.130.86.71 — Adapter + Server 合一部署
# SSH：root / dev@BDW2026
# 公网 gRPC 入口：8088
#
# ⚠️ uenv-adapter-core 已内嵌 ControlPlaneService（Worker Register）与
#    AdapterCoreService（VeRL），与独立 uenv-server 二选一，勿同端口重复启动。
#
# 启动：
#   export UENV_ADDR=0.0.0.0:8088
#   /home/uenv/target/release/uenv-adapter-core
#
# Worker server.endpoint / 7142 Adapter 均指向：8.130.86.71:8088

process: uenv-adapter-core
bind_env: UENV_ADDR
bind: "0.0.0.0:8088"
public_endpoint: "8.130.86.71:8088"
