# 阿里云 8.130.75.157 — Adapter + Server 合一部署（原 8.130.86.71 已迁移）
# SSH：root / dev@BDW2026
# 公网 gRPC 入口：8088；轨迹 HTTP：8077
#
# ⚠️ uenv-adapter-core 已内嵌 ControlPlaneService（Worker Register）与
#    AdapterCoreService（VeRL），与独立 uenv-server 二选一，勿同端口重复启动。
#
# 启动（轨迹 v2.2 见 config/uenv-server.trajectory.env.example）：
#   source /root/.uenv-server.env
#   nohup /home/uenv/target/release/uenv-adapter-core >> /var/log/uenv/adapter-core.log 2>&1 &
#
# Worker server.endpoint / 7142 Adapter 均指向：8.130.75.157:8088

process: uenv-adapter-core
bind_env: UENV_ADDR
bind: "0.0.0.0:8088"
public_endpoint: "8.130.75.157:8088"
trajectory_http: "0.0.0.0:8077"
