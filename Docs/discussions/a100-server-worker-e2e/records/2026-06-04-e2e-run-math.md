# 2026-06-04 Server-Worker 实机联调记录（math 语义）

> **结果**：✅ 通过  
> **范围**：Worker ↔ Server（不含 Bridge）；`env_type=math` + `payload.dataset=gsm8k`  
> **Hub**：Worker 启动时已 pull math manifest（M-5）；Episode 热路径不依赖 Hub

---

## 环境

| 项 | 值 |
|----|-----|
| 机器 A | `10.10.20.143:50051` — uenv-server |
| 机器 B | `10.10.20.142:50052` — uenv-worker |
| Worker 配置 | `Docs/discussions/a100-server-worker-e2e/config/uenv-worker.e2e.yaml` |
| 客户端 Mock | grpcurl `UEnvService/SubmitEpisode` |
| 代码路径 | `/root/UEnv`（两台一致） |

---

## 验收结果

| 检查项 | 结果 |
|--------|------|
| A→B `:50052` 网络 | ✅ |
| Worker 注册 `10.10.20.142:50052` | ✅ |
| 心跳 | ✅ 持续 ack |
| Episode #1 SubmitEpisode | ✅ `status=completed`, `totalReward=1`, `integrityVerified=true` |
| Episode #2 SubmitEpisode（Warm 池） | ✅ 同上；`warmup_hit=true` |
| Server `control_plane_report_result` | ✅ |
| Worker `report_result` | ✅ |
| Metrics `uenv_episode_total` | ✅ ≥ 3 |

---

## Episode 样例（#2）

```json
{
  "status": "completed",
  "summary": { "totalReward": 1, "totalSteps": 1 },
  "integrityVerified": true
}
```

---

## 关键日志（机器 B Worker）

```
control_plane_mode_remote endpoint=10.10.20.143:50051
register worker_id=auto server_epoch=1
hub_manifest_pulled env_type=math version=1.0.0
dispatch_received episode_id=math-e2e-run-...-002
episode_phase phase=acquire warmup_hit=true instance_id=math-2
dispatch_completed warmup_hit=true reward=1.0
report_result reward=1.0
```

---

## 复现命令

```bash
# 机器 A
bash Docs/discussions/a100-server-worker-e2e/scripts/run-e2e-acceptance.sh 127.0.0.1:50051

# 或单次
bash Docs/discussions/a100-server-worker-e2e/scripts/submit-episode-grpcurl.sh 127.0.0.1:50051
```

---

## 与 2026-05-30 历史记录差异

| 项 | 5/30 | 本次 |
|----|------|------|
| `env_type` | `gsm8k` | **`math`** |
| 插件 | `uenv-gsm8k-plugin` | **`uenv-math-plugin`** |
| Hub | 未启用 | Worker 启动 pull math manifest |
| payload | 无 dataset 字段 | `"dataset":"gsm8k"` |
