# 更新日志

本文件记录 UEnv 仓库**已完成的代码与工程变更**，供回溯与协作对齐。设计讨论与未落地规划仍以各专题文档为准。

## 文档编码注意事项（PowerShell / Windows）

2026-05-25 起，`Docs/` 下 Markdown 曾因 **PowerShell 写入未指定编码** 出现 UTF-8 损坏（如 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 乱码、本文件部分条目乱码）。后续修改请遵守：

1. **文件编码**：`Docs/` 中文 Markdown 统一 **UTF-8（无 BOM）**。
2. **读通常安全**：`Get-Content` 只读一般不会破坏文件。
3. **写必须显式编码**：`Set-Content`、`Add-Content`、`Out-File` 等**重写字节**的命令必须带 `-Encoding utf8NoBOM`（PowerShell 7+）或等效 UTF-8 无 BOM 策略；**禁止**依赖系统默认编码。
4. **已确认高风险命令**（未带 `-Encoding` 时）：
   - `Set-Content -Path 'Docs\worker-pool-mvp-checklist.md' -Value ($head + $mid + $tail)`
   - `... | Add-Content -Path 'Docs\更新日志.md'`
5. **损坏形态与处置**：
   - 第一阶段：UTF-8 多字节字符**最后一字节变为 `?`**；
   - 第二阶段：若用脚本“猜续字节”修复，会产生**形似中文的错字**（如「骨枀」「插什」）——**禁止**再用此类脚本；
   - 正确做法：以未损坏副本（如 `worker-pool-mvp-checklist_1.md`）或版本历史合并还原。
6. **修改后自检**：保存后打开文件或 `grep` 抽查中文关键词，确认无 `?`、无错字、Markdown 符号（`§`、`→`、`**`）正常。
7. **本次事故（2026-05-25，本文件全文 `?`）**：
   - **现象**：对 [`更新日志.md`](./更新日志.md) 做内容整理（时间倒序重排）后，**全部中文变为 `?`**，英文、日期、路径、代码标识仍可读。
   - **推断根因**：文件被**以非 UTF-8 编码（多为 Windows 系统默认 ANSI/GBK）或错误解码后再写入**整文件覆盖；UTF-8 多字节序列无法映射到目标单字节页时，被替换为 `?`（典型“不可逆全损”，不同于仅末字节损坏）。
   - **高风险场景**：自动化工具 / Agent **批量重写** `.md` 时未声明 UTF-8；PowerShell `Set-Content` / `Out-File` 无 `-Encoding`；将已为 `?` 的文件当作“已修复”再次保存；**对中文文件名的目标路径直接写入时也可能触发错误编码管道**（本文件两次复现）。
   - **处置**：**禁止**在 `?` 占位文本上继续编辑或“猜字修复”；须用未损坏副本、Git 历史、或 `python scripts/restore_update_log.py` 等**显式 UTF-8 写出**整文件恢复。
   - **预防**：保存前目视标题与首段中文；大段改写后立刻 `grep` 关键词（如 `更新日志`、`M1.4`）；优先用编辑器“UTF-8”保存；脚本写入用 `path.write_text(..., encoding='utf-8')` 或 PowerShell `-Encoding utf8NoBOM`。

---

## 更新规则

1. **按时间倒序**：最新日期在最上方；同一日期内按时间倒序（新的在上）。
2. **日期 + 时间**：每条记录使用二级标题 `## YYYY-MM-DD HH:MM`（本地时间，24 小时制）。
3. **按条列举**：每条变更一行，使用 `-` 列表；一条一事，写清「做了什么 / 影响范围」。
4. **只记已完成**：进行中的工作不写；合并 PR 或本地验收通过后再追加。
5. **可引用文档**：若变更依据某设计/清单，在条目中用相对链接注明（如 `worker-pool-pre-mvp-architecture-adjustment.md`）。
6. **不写 MVP 业务细节占位**：框架级骨架与契约对齐记本日志；Mock 主动 Dispatch、Episode 执行等运行时逻辑记对应里程碑完成时再追加。
7. **追加条目时的编码**：向本文件追加内容时，**禁止**使用未指定 `-Encoding` 的 `Add-Content`；优先用编辑器保存，或 `Add-Content -Encoding utf8NoBOM`；或由 `restore_update_log.py` 合并后整文件写出。

---

---

---

---

## 2026-07-18 23:02

- **Adapter DSCodeBench 评测说明归档**：新增 [`adapter/DSCodeBench-UEnv评测Worker核验问题说明.md`](./adapter/DSCodeBench-UEnv评测Worker核验问题说明.md)，整理 UEnv 全量 DSCodeBench 结果中需要 Worker 侧核验的 code env 依赖、harness 执行环境与失败返回口径问题。
- **DSCodeBench 文档口径修正**：更新 [`../uenv-bridge/docs/任务测评/DSCodeBench代码生成基线评测.md`](../uenv-bridge/docs/任务测评/DSCodeBench代码生成基线评测.md)，区分直接 vLLM 历史 baseline 与本轮 UEnv thinking 全量结果，避免不同 `MAX_TOKENS` / `THINKING_TOKEN_BUDGET` 配置被误读为同口径对比。

---

## 2026-06-27 18:00

- **scripts 重组**：`services/openhands-runner/` → `scripts/openhands/`；`services/uenv-llm-gateway/` → `scripts/uenv-llm-gateway/`；删除空 `services/` 目录。
- **scripts 清理**：删除 2026-06 联调临时脚本（`_*.py`、`deploy_worker_*`、`e2e_chain_*`、`deploy-swe-pro-7143.sh`、`remote-deploy-pro-7143.sh` 等仍用 `:28999` 的过期脚本）；保留 OpenHands / SWE Pro / LLM 网关现行脚本。
- **路径同步**：`deploy-openhands-20877.sh`、`deploy-uenv-llm-7142.sh` 及文档中的 `services/` 引用已改为 `scripts/` 子目录。

---

## 2026-06-27 16:30

- **SWE-bench Pro + OpenHands 文档整合**：新增 [`260627-swe-openhands-integration-plan.md`](./260627-swe-openhands-integration-plan.md)（方案）与 [`260627-swe-openhands-acceptance-report.md`](./260627-swe-openhands-acceptance-report.md)（验收）；旧联调/迁移/验收文档改为归档 stub。
- **config 清理**：删除 `uenv-worker.deploy-7142.yaml`、`openhands-llm-7142.json.example`；OpenHands 配置统一为 `openhands-20877.*`。
- **scripts 清理**：删除 `deploy-openhands-7142.sh`、`run-openhands-pro-7142.sh` 等 7142 过期脚本；保留 `deploy-openhands-20877.sh` / `run-openhands-pro-20877.sh`。

---

- **A100 四端 GSM8K 实机验收（AgentLoop + 1-step GRPO）**：在 7142（VeRL/bridge）、7143（worker）、`8.130.86.71:8088`（adapter-core，仅拉起未改代码）完成部署与联通；7143 编译部署 OpenRouter 版 `uenv-worker` / `uenv-math-plugin`，上传 `config/uenv-worker-llm.env`；7142 同步 `uenv-bridge` 并安装容器内 `grpcio>=1.80`。
- **链路 smoke 通过**：7142 执行 `verify_pre_rollout_rust_core_loop.py` → `8.130.86.71:8088` → 7143 Worker → OpenRouter → math 插件；`uenv_status=completed`，Worker `report_result` 正常。
- **首次 1-step GRPO（`max_response_length=32`）**：训练 `1/1` 完成，但 Worker **`reward=0.0`**、VeRL `critic/rewards/mean=0.0`；**非链路失败**，系 GSM8K 规则判分「答错」。
- **根因（reward=0）**：
  - VeRL `data.max_response_length=32` 传入 Worker 为 `max_new_tokens=32`，`response_length/clip_ratio=1.0`（顶满截断，非溢出）；
  - Bridge→Worker 的 `question` 曾仅用 `extra_info.question`（无 `Let's think step by step... ####` 指令）；
  - `qwen/qwen3-max` **无**独立 thinking 模式（与 `qwen3-max-thinking` 不同），长推理风格在 32 token 内无法写到 `#### 72`。
- **对比跑（`DATA_MAX_RESPONSE_LENGTH=256` + 完整 prompt）**：Worker **`reward=1.0`**，VeRL **`critic/rewards/mean=1.0`**，`response_length≈162`、`clip_ratio=0.0`；插件 `episode.json` 题干已含 `####` 指令，标准答案 `72`（Natalia 样本）。
- **代码/脚本调整**：
  - [`verl_agent_loop.py`](../uenv-bridge/src/uenv/bridge/verl_agent_loop.py) 新增 `_worker_llm_question`，将 parquet 完整 user prompt 写入 `metadata.extra_info.question`；
  - [`run_verl_grpo_1step_with_uenv_agent_loop.sh`](../uenv-bridge/scripts/run_verl_grpo_1step_with_uenv_agent_loop.sh) 支持 `DATA_MAX_RESPONSE_LENGTH`；容器启动段安装 `grpcio>=1.80`（修复 stub 版本不匹配）；
  - 7142 对比脚本 `/root/UEnv/run-single-gsm8k-compare-7142.sh`（`SAMPLE_COUNT=1`，GPU 4）。
- **实机踩坑**：Windows `scp` 的 `.env` / `run.sh` 易带 CRLF（导致 `plugin math-1 not ready`）；7142 GPU0 显存占满需 `CUDA_VISIBLE_DEVICES_IN_CONTAINER=4`；日志见 7142 `/var/log/uenv/single-gsm8k-grpo-gpu4-v3.log`（reward=0）、`/var/log/uenv/single-gsm8k-compare-256.log`（reward=1）。

## 2026-06-12 20:00

- **VeRL 集成路径切换为 AgentLoop 预 rollout**：移除 [`verl.py`](../uenv-bridge/src/uenv/bridge/verl.py)、[`verl_reward_manager.py`](../uenv-bridge/src/uenv/bridge/verl_reward_manager.py)；新增 [`verl_agent_loop.py`](../uenv-bridge/src/uenv/bridge/verl_agent_loop.py)、[`agent_loop_clients.py`](../uenv-bridge/src/uenv/bridge/agent_loop_clients.py)；VeRL 通过 `default_agent_loop=uenv_agent` 将 rollout 交给 UEnv，从 `AgentLoopOutput.reward_score` 取 reward。
- **Bridge L1 映射落地（B-1～B-6）**：[`core.rs`](../uenv-bridge/core/src/core.rs) 内联 `sample_to_worker_payload` / `sample_to_worker_reward_config`；`question`、`dataset=gsm8k` 规范化、`rubric`→`rule_reward`、`response_text` 映射；单测 `execute_batch_maps_verl_math_payload_to_worker_contract`。
- **默认真实链路**：`UENV_AGENT_LOOP_CLIENT`、`UENV_ADAPTER_CORE_BACKEND` 默认改为 `rust_core` / `server`；[`run_verl_grpo_1step_with_uenv_agent_loop.sh`](../uenv-bridge/scripts/run_verl_grpo_1step_with_uenv_agent_loop.sh)、[`verify_pre_rollout_rust_core_loop.py`](../uenv-bridge/scripts/verify_pre_rollout_rust_core_loop.py) 去除 `fake` / `static_rollout` 默认。
- **Worker LLM 专用 `.env`**：新增 [`config/uenv-worker-llm.env.example`](../config/uenv-worker-llm.env.example)、[`llm.rs`](../uenv-worker/src/llm.rs)；启动时加载 `config/uenv-worker-llm.env`（`UENV_WORKER_LLM_ENV` 可覆盖）；`ModelClient` 以 `.env` 为 LLM 权威配置。
- **ModelClient 修复（W-2）**：已配置 LLM 时禁止 `rule_reward` 短路作弊；仅无 LLM、无 `question` 时保留 grpcurl 联调捷径；[`payload.rs`](../uenv-worker/src/episode/payload.rs) 二次规范化 `dataset`。
- **轨迹回传**：math 插件与 [`executor.rs`](../uenv-worker/src/episode/executor.rs) 在 `step.info` 写入 `response_text`。
- **文档**：重写 [`260608-verl-gsm8k-real-testing-adjustments.md`](./260608-verl-gsm8k-real-testing-adjustments.md)（AgentLoop 路径、LLM 分工、验收清单）；更新 [`260609-worker-full-chain-integration-summary.md`](./260609-worker-full-chain-integration-summary.md) §1.2 过时说明。

## 2026-06-11 18:30

- **功能边界重构（平台 vs 环境）**：GSM8K 判分从 `uenv-worker/src/episode/math_scoring.rs` 迁至环境制品 crate [`plugins/math/`](../plugins/math/)（`uenv-math-env`），落地 [`backends/gsm8k/scoring.rs`](../plugins/math/src/backends/gsm8k/scoring.rs)；删除 Worker episode 层领域逻辑。
- **判分权威调整（P-4）**：[`reward_engine.rs`](../uenv-worker/src/episode/reward_engine.rs) 默认采信 math 插件 `step.reward`；仅 `reward_config.scorer=worker` 时由平台做通用精确比对（headless 单测，不含 GSM8K 提取）。
- **math 插件**：[`uenv-math-plugin.rs`](../uenv-worker/src/bin/uenv-math-plugin.rs) 依赖 `uenv-math-env::gsm8k`，不再 `use uenv_worker::episode::*`。
- **workspace**：根 [`Cargo.toml`](../Cargo.toml) 新增 `plugins/math` 成员。
- **文档**：[`260608-verl-gsm8k-real-testing-adjustments.md`](./260608-verl-gsm8k-real-testing-adjustments.md) 新增 §1.4 功能边界表，更新 W-3 / P-3～P-5 表述。

## 2026-06-11 17:00

- **Worker GSM8K 真实判分能力补齐（W-1～W-4、W-6～W-9、P-1～P-4）**：按 [`260608-verl-gsm8k-real-testing-adjustments.md`](./260608-verl-gsm8k-real-testing-adjustments.md) 落地 `uenv-worker` 侧缺口；VeRL 路径首步优先读 `payload.response_text`，无 response 时才走 LLM 或 `rule_reward` 联调捷径。
- **ModelClient / RewardEngine**：[`model_client.rs`](../uenv-worker/src/episode/model_client.rs) 支持 `response_text` 优先与多步 `step_index`；[`reward_engine.rs`](../uenv-worker/src/episode/reward_engine.rs) 兼容 `rubric_config.ground_truth` 与 GSM8K `####` 提取判分；新增 [`math_scoring.rs`](../uenv-worker/src/episode/math_scoring.rs)、[`payload.rs`](../uenv-worker/src/episode/payload.rs)。
- **Episode 执行与插件传题**：[`executor.rs`](../uenv-worker/src/episode/executor.rs) 实现 `execute_episode` 多步主循环（`max_steps`/`terminated`）；reset 前经 UDS 侧 `{uds}.episode.json` 注入 `question`/`dataset`/`target`（W-4）；[`uenv-math-plugin.rs`](../uenv-worker/src/bin/uenv-math-plugin.rs) 按题 reset + `dataset=gsm8k` 判分；**冻结** `RewardEngine` 为唯一权威判分源（P-4）。
- **控制面与流式上报**：[`control_plane/client.rs`](../uenv-worker/src/control_plane/client.rs) 心跳 `load` 上报真实活跃 Episode 数（W-6）、`RegisterWorker.resource` 填充 `ResourceSpec`（W-7）；[`worker_service.rs`](../uenv-worker/src/grpc_server/worker_service.rs) 多步 `StreamReport` 填 `report_type`/`correlation_id`/`worker_active_episodes`（W-8）。
- **清单同步**：更新 [`260608-verl-gsm8k-real-testing-adjustments.md`](./260608-verl-gsm8k-real-testing-adjustments.md) Worker/math 插件项为 ✅；Bridge `l1_mapping`（B-1～B-6）与全栈实机验收仍待后续。

## 2026-06-04 17:00

- **Step 1b A100 实机联调完成（math 语义）**：在两台 A100（A `10.10.20.143:50051` Server + Hub，B `10.10.20.142:50052` Worker）完成 **Worker ↔ Server** 全链路验收；`env_type=math` + `payload.dataset=gsm8k`；grpcurl Mock `SubmitEpisode`；详见 [`records/2026-06-04-e2e-run-math.md`](./discussions/a100-server-worker-e2e/records/2026-06-04-e2e-run-math.md)。
- **验收结果**：连续 2 次 Episode 均 `status=completed`、`totalReward=1.0`、`integrityVerified=true`；第 2 次 `warmup_hit=true`（Warm 池复用）；Register → Heartbeat → Dispatch → Report 日志交叉验证通过；`uenv_episode_total ≥ 3`；Worker 启动时 Hub manifest pull 成功（`hub_manifest_pulled env_type=math`）。
- **uenv-server 恢复 gRPC 二进制入口**：883ba51 重构为 lib-only 后缺失 `main.rs` 与 `UEnvService` gRPC 层，导致 grpcurl 无法调用；新增 [`uenv-server/src/main.rs`](../uenv-server/src/main.rs)（`UEnvService` + `AdminService` + `ControlPlaneService` 同端口）；[`service.rs`](../uenv-server/src/service.rs) 新增 `UEnvServiceImpl`，委托 [`UEnvEpisodeService`](../uenv-server/src/service.rs) 执行 `SubmitEpisode`；[`Cargo.toml`](../uenv-server/Cargo.toml) 补 `[[bin]]`、`clap`、`tokio-stream`。
- **uenv-worker 实机修复**：[`warmup_pool.rs`](../uenv-worker/src/pool/warmup_pool.rs) spawn 后 `wait_plugin_ready`（health_check 轮询），消除插件 UDS 未就绪即 reset 的竞态；[`model_client.rs`](../uenv-worker/src/episode/model_client.rs) 恢复 `rule_reward` 回退（读 `reward_config.target` 作 action），e2e 无需 `model_endpoint`。
- **实机运维脚本（a100-server-worker-e2e）**：新增 [`remote-build.sh`](./discussions/a100-server-worker-e2e/scripts/remote-build.sh)、[`start-server-stack.sh`](./discussions/a100-server-worker-e2e/scripts/start-server-stack.sh)（Hub `UENV_HUB_AUTH__REQUIRE_TOKEN=false` + Server）、[`start-worker.sh`](./discussions/a100-server-worker-e2e/scripts/start-worker.sh)、[`stop-uenv.sh`](./discussions/a100-server-worker-e2e/scripts/stop-uenv.sh)、[`run-e2e-acceptance.sh`](./discussions/a100-server-worker-e2e/scripts/run-e2e-acceptance.sh)；[`uenv-worker.e2e.yaml`](./discussions/a100-server-worker-e2e/config/uenv-worker.e2e.yaml) 启用 Hub（`http://10.10.20.143:8080`）；[`sync-from-dev.ps1`](./discussions/a100-server-worker-e2e/scripts/sync-from-dev.ps1) 修复 PowerShell `&&` 解析错误。
- **工程细节**：[`plugins/math/run.sh`](../plugins/math/run.sh) 统一 LF（Windows tar 同步后避免 `bash\r` 导致插件无法启动）；Worker 二进制位于 workspace 根 `target/release/`（非 `uenv-worker/target/`）。
- **与 2026-05-30 M7 差异**：历史记录为 `env_type=gsm8k` + `uenv-gsm8k-plugin`；本次为 **math 语义正式实机验收**，关闭 [`260530-full-stack-integration-gaps.md`](./260530-full-stack-integration-gaps.md) Step 1b「待 A100 复验」。
- **待后续**：Bridge serve 三联调（P0-8）；Hub 深度 pull / 制品同步（P2-2）。

## 2026-06-01 14:00

- **Worker 预热池按需拉起 + Hub 对接补全**：[`WarmupPool`](../uenv-worker/src/pool/warmup_pool.rs) 在 `acquire`/`fill_pool` 前经 [`EnvResolver`](../uenv-worker/src/hub/env_resolver.rs) 确保 manifest（本地 `plugins/` 优先，Hub `GET .../versions/latest` 补元数据）；缺 Warm 实例时 Worker 自行 `spawn`，Episode 结束 `release` 回 **Warm** 复用（非待预热队列）。新增 `pool.prewarm_on_startup` / `UENV_PREWARM_ON_STARTUP`（默认 `false`）。文档：[`260531-worker-hub-server-protocol-integration.md`](./260531-worker-hub-server-protocol-integration.md)、[`260530-full-stack-integration-gaps.md`](./260530-full-stack-integration-gaps.md)。

## 2026-05-31 22:00

- **MathEnv 跨层落地（M-1～M-6，代码完成）**：按 [`worker-pool-layer-design.md`](./worker-pool-layer-design.md) §9.4 实施顺序完成 gsm8k→math 全栈迁移；跨层语义冻结为 **`env_type=math`** + **`payload.dataset=gsm8k`**（GSM8K 为 math 下 benchmark，不再作为独立 env_type）。
- **M-1 Worker 注册 math**：`config/uenv-worker.yaml` / `uenv-worker.json` / `worker.example.toml` 默认 `env.types: ["math"]`；`RegisterWorker.supported_env_types` 上报 `math`；单测与集成测（`m2`～`m6`、`wal`）统一 math 语义。
- **M-2 插件目录收敛**：新增 `plugins/math/`（`manifest.yaml` + `run.sh`）；删除 `plugins/gsm8k/`；插件二进制 `uenv-math-plugin`（`uenv-worker/src/bin/uenv-math-plugin.rs`）取代 `uenv-gsm8k-plugin`；环境变量 `UENV_MATH_PLUGIN_BIN` 取代 `UENV_GSM8K_PLUGIN_BIN`。
- **M-3 fixture / e2e**：新增 `fixtures/math/` 与 `gen_math_fixture.rs`；`fixtures/gsm8k/README.md` 注明已迁移至 math；Mock Scheduler 默认 fixture 路径、`m1_contract_chaos_tests`、e2e 脚本（[`submit-episode-grpcurl.sh`](./discussions/a100-server-worker-e2e/scripts/submit-episode-grpcurl.sh)、[`machine-env.sh`](./discussions/a100-server-worker-e2e/scripts/machine-env.sh)、[`uenv-worker.e2e.yaml`](./discussions/a100-server-worker-e2e/config/uenv-worker.e2e.yaml)）统一 `env_type=math` + `payload.dataset=gsm8k`。
- **M-4 Bridge 回退 gsm8k→math**：[`verl.py`](../uenv-bridge/src/uenv/bridge/verl.py) / [`verl-adapter.yaml`](../uenv-bridge/configs/verl-adapter.yaml) `task_to_env_type.gsm8k: math`；[`l1_mapping.rs`](../uenv-bridge/core/src/l1_mapping.rs) 写入 `payload.dataset`（默认 `gsm8k`）；VeRL 单测断言 `env_type=math`。
- **M-5 Worker Hub pull**：新增 [`uenv-worker/src/hub/mod.rs`](../uenv-worker/src/hub/mod.rs)；`runtime.rs` 启动时可选 `GET /api/v1/envs/{env_type}/versions/latest`；配置 `hub.enabled` / `UENV_HUB_ENDPOINT`；Hub 不可达时降级本地 manifest。
- **M-6 移除 gsm8k alias**：删除 `plugins/gsm8k`、`UENV_GSM8K_PLUGIN_BIN`；`l1_mapping` 不再做 `env_type` 层 gsm8k→math 归一化；[`proto/uenv/v1/episode.proto`](../proto/uenv/v1/episode.proto) Phase 0 注释改为 `math`。
- **文档同步**：[`260530-full-stack-integration-gaps.md`](./260530-full-stack-integration-gaps.md) Step 1b 标记代码完成（待 A100 复验）；[`worker-pool-layer-design.md`](./worker-pool-layer-design.md) §9.4 M-1～M-6 全部 ✅；[`uenv-worker/README.md`](../uenv-worker/README.md) 插件路径与 fixture 示例更新。
- **验证**：`cargo test -p uenv-mock-scheduler --test m1_contract_chaos_tests` **8 passed**；`uenv-bridge/core` **10 passed**；`uenv-worker` hub 单测 **1 passed**；VeRL adapter **12 passed**（Windows 子进程 stub 1 项已知失败）。
- **文档同步（协议口径）**：[`PROTOCOL.md`](../PROTOCOL.md) v1.1 — `env_type=math`、`payload.dataset`、M-5 Hub pull；[`proto/README.md`](../proto/README.md)；[`260530-full-stack-integration-gaps.md`](./260530-full-stack-integration-gaps.md) §1/Y1/P2-2；[`secrets/README.md`](../secrets/README.md) §6 移除 proto 分裂过时表述。
- **待后续**：~~A100 math 复验~~（✅ 见 2026-06-04 17:00）；Bridge serve 三联调（P0-8）；Hub 深度 pull（P2-2）。

## 2026-05-31 17:00

- **MathEnv 跨层规划（PRD v7.2 对齐）**：[`worker-pool-layer-design.md`](./worker-pool-layer-design.md) 升至 **v1.4**；Phase 0 由「单 `env_type=gsm8k`」调整为 **通用 MathEnv（`env_type=math`）**；GSM8K 降为 math 下 `payload.dataset` benchmark；新增 §9.4 迁移清单 **M-1～M-6**；配置示例与 `UENV_ENV_TYPES` 默认改为 `math`。
- **全栈缺口清单修订**：重写 [`260530-full-stack-integration-gaps.md`](./260530-full-stack-integration-gaps.md) — 新增 §0.2 MathEnv 对齐清单；**撤回 G7**（`gsm8k→gsm8k` 为临时方案）；**P0-4** 改为规划项；新增阻塞 **B0**；关闭 **Y2**（Hub 已有 `math` seed）；验收顺序插入 **Step 1b**（math 收敛后再 Bridge 三联调 / VeRL Layer 4）。
- **依据文档**：对齐 [`uenv-design-prd-v7.2.md`](./uenv-design-prd-v7.2.md) §3.2.5 内置环境矩阵（MathEnv 验证型）与 Hub seed（`uenv-hub-core/src/seed.rs` 已有 `math`）。
- **代码状态说明（已被 22:00 条目取代）**：M7 实机（2026-05-30）仍使用 `env_type=gsm8k`，保留为历史证据；M-1～M-6 代码迁移于同日 22:00 落地完成。

## 2026-05-31 14:00

- **Bridge serve mode（P0-2～P0-6）**：`uenv-bridge/core` 新增 `serve_client.rs`（`UEnvServeEpisodeService` gRPC 调 `UEnvService.SubmitEpisode`）、`l1_mapping.rs`（`request_id`↔`episode_id`、VeRL payload → Worker `question` + `rule_reward.target`）；`main.rs` 新增 `UENV_ADAPTER_CORE_REWARD_MODE=serve` 与 `UENV_SERVER_ENDPOINT`。
- **Proto 构建升级**：`core/build.rs` 生成 L1 `uenv.v1` gRPC client（`server.proto` + `scheduler.proto`）；依赖升至 **tonic/prost 0.14**；`Cargo.toml` 增加 `[workspace]` 独立构建。
- **Bridge env_type 临时对齐（待 M-4 回退）**：`verl.py` / `verl-adapter.yaml` 将 `gsm8k→gsm8k`；`env_type_for_sample` 优先匹配 `data_source`；`run_verl_grpo_1step_with_bridge_reward.sh` 导出 `UENV_SERVER_ENDPOINT`。
- **验证**：`cargo test -p uenv-adapter-core`（workspace 外于 `uenv-bridge/core/`）**10 passed**；VeRL adapter 单测 `env_type=gsm8k` 相关用例通过。
- **缺口文档（首版）**：创建并更新 [`260530-full-stack-integration-gaps.md`](./260530-full-stack-integration-gaps.md) — M7 归入 G3；标记 serve mode 代码就绪；P0-1～P0-6 勾选；新增 P0-8 三联调 smoke（**待 math 语义与实机**）。

## 2026-05-30 18:30

- **M7 真实 Server 实机联调完成**：在两台 A100 Linux 测试机上完成 `uenv-server`（机器 A `10.10.20.143:50051`）+ `uenv-worker`（机器 B `10.10.20.142:50052`）跨机全链路验收；代码独立部署于 `/root/UEnv`，Bridge 用 grpcurl Mock `SubmitEpisode`，Hub 跳过。
- **测试方法**：开发机 tar+scp 同步代码与 `src/gen/` → 双机 release 编译 → A 启动 Server、B 启动 Worker（`uenv-worker.e2e.yaml` + `UENV_GSM8K_PLUGIN_BIN`）→ A 执行 `submit-episode-grpcurl.sh`；详见 [`discussions/a100-server-worker-e2e/README.md`](./discussions/a100-server-worker-e2e/README.md) 与 [`records/2026-05-30-e2e-run.md`](./discussions/a100-server-worker-e2e/records/2026-05-30-e2e-run.md)。
- **验收结果**：Episode `gsm8k-e2e-001` 返回 `status=completed`、`summary.total_reward=1.0`、`integrity_verified=true`；Server/Worker 日志交叉验证 Register → Dispatch → Report 完整；`uenv_episode_total 1`；`warmup_hit=true`。
- **代码修复**：`uenv-worker/src/plugin/arpc/mod.rs` 补齐 tonic 0.14 UDS `TokioIo` 包装；`submit-episode-grpcurl.sh` 对 proto `bytes` 字段使用 base64。
- **清单同步**：更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) — M7 联调清单全部勾选，新增「M7 真实 Server 实机联调结果（2026-05-30）」；M9+ 跨机真实链路项标记已完成。

## 2026-05-28 17:31
- **下一阶段规划文档重写**：按你的反馈重写 [`260528-1722-worker-next-phase-plan.md`](./260528-1722-worker-next-phase-plan.md)，从泛化描述改为“design 对照型计划”。
- **对照依据增强**：新版本按 [`worker-pool-layer-design.md`](./worker-pool-layer-design.md) 关键章节（控制面、插件实例、预热池、后端、容错、观测）逐项给出“当前状态/缺口/补齐动作/验收标准”。
- **补充沙箱与通用环境细节**：新增“沙箱与通用环境执行能力”章节，明确执行级别、重试边界、隔离策略与验收口径；同步保留 MathEnv 扩展与 Hub 协同接入规划。

## 2026-05-28 17:22
- **Gap 文档口径更新**：重写 [`260526-1927-worker-pool-mvp-vs-full-design-gap.md`](./260526-1927-worker-pool-mvp-vs-full-design-gap.md)，将阶段定义调整为“Worker 层 MVP 阶段完成”，并基于当前实际状态重述已实现能力、未实现边界与跨层依赖。
- **新增下一阶段规划文档**：创建 [`260528-1722-worker-next-phase-plan.md`](./260528-1722-worker-next-phase-plan.md)，明确 MVP 后补齐方向，重点包含 MathEnv 扩展、多环境负载执行、真实 Server 跨机联调收口与 Hub 接入准备。
- **规划边界明确**：在新文档中区分本阶段优先项与非阻塞项，避免将生产化全量能力一次性并入当前迭代范围。

## 2026-05-28 17:00
- **清单改版（M9+）**：将 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 的“`M9+：增强项（非 MVP 阻塞）`”从排期表改写为“当前实现情况总结 + 未实现与跨层待补全”。
- **总结口径调整**：改为功能需求视角描述当前交付能力（预热池、默认参数、派发执行与容错可用性），不展开底层技术实现细节。
- **待补全边界明确**：在同节新增当前未完成项与跨层依赖说明，包括真实 `uenv-server` 跨机链路验收未完成、Hub 层未真实接入、Worker 仍处 MVP 后续需升级覆盖。

## 2026-05-28 16:51
- **M7 本机预联调回归**：执行 `cargo test -p uenv-mock-scheduler --test m1_contract_chaos_tests -- --nocapture`，结果 `8 passed, 0 failed`；其中 `m16_register_heartbeat_dispatch_report_chain` 覆盖 Register → Heartbeat →（Scheduler 主动）Dispatch → ReportResult，确认本机预联调链路可跑通。
- **M8 断连策略验证**：在 `uenv-worker/src/grpc_server/worker_service.rs` 新增 `m8_disconnect_policy_reject_returns_unavailable` 与 `m8_disconnect_policy_queue_does_not_fail_on_connection_gate`，验证 `UENV_DISPATCH_ON_DISCONNECT=reject|queue` 在控制面断连时的策略分支。
- **M8 复用 M1.7 场景回归**：在 `uenv-mock-scheduler/tests/m1_contract_chaos_tests.rs` 新增 `m17_partial_stream_interruption_report_result_still_ack`，并与 `report_result_retry`、`heartbeat_timeout` 场景一起通过；对应命令结果为 `8 passed, 0 failed`。
- **清单同步**：更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md)，完成 M8 两个待办勾选，并追加 “M7 本机预联调结果（2026-05-28）” 与 M8 现状总结补充说明。

## 2026-05-28 16:19
- **M7 决议落文**：在 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 新增“`M7 预联调决议（2026-05-28）`”，明确当前先采用本机 IP+端口做 remote 形态预联调，并注明该结果不替代真实 `uenv-server` 联调验收。
- **Worker 文档同步**：更新 [`../uenv-worker/README.md`](../uenv-worker/README.md) 新增“本机预联调（当前决议）”段落，固定环境变量切换方式与验收边界，避免将预联调误判为 M7 完成。
- **配置样例落地**：更新 [`../config/uenv-worker.yaml`](../config/uenv-worker.yaml) 与 [`../config/uenv-worker.json`](../config/uenv-worker.json)，将 `server.endpoint` 调整为本机预联调地址 `192.168.56.1:50051`，并保留 Worker 回连监听说明。

## 2026-05-28 15:08
- **M5（RewardEngine）**：在 `uenv-worker` 新增 `episode/reward_engine.rs`，实现最小 `RuleReward` 语义：当 `reward_config.type=rule_reward` 时按 `target` 与 action 文本匹配计算奖励（命中=1.0，未命中=0.0），其他类型回退插件返回奖励。
- **执行链路接入**：`EpisodeExecutor` 接入 `RewardEngine`，并将 `StepRecord.reward`、`Trajectory.total_reward`、`EpisodeResult.summary.total_reward`、`StreamReport.current_reward` 统一改为规则引擎输出，确保结果与规则奖励口径一致。
- **测试与验证**：新增 `RewardEngine` 单元测试 2 个（命中/未命中），执行 `cargo test -p uenv-worker reward_engine` 通过（2 passed）。
- **清单同步**：更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md)，将 M5 的 `RewardEngine` 与“Mock 主动 Dispatch → Result 对比 reward/status”两项标记为已完成。

## 2026-05-27 10:40
- **M8（容错：WAL 持久化 + 重连）**：在 uenv-worker 落地 WalWriter，按 uenv.v1.WalRecord 持久化 EpisodeResult，补齐 `request_checksum` / `result_checksum`，并新增 CRC32 校验；损坏 WAL 记录会跳过并打 ERROR。
- **上报语义**：DispatchEpisode 完成后改为“先写 WAL、后 ReportResult”，ACK 成功后删除 WAL，确保控制面短时断连不丢结果。
- **重连重放**：新增 ControlPlane 后台 WAL replay loop，对 pending 记录按指数退避（500ms 到 10s）持续重试，直到 ACK；支持连接状态感知。
- **断连策略**：新增 `UENV_DISPATCH_ON_DISCONNECT`（`reject|queue`），可配置断连时拒绝新 Dispatch 或继续接收并依赖 WAL 重放。
- **观测性**：新增并接入指标 uenv_wal_pending_records，在 WAL 落盘/ACK 删除后实时更新。
- **测试与验证**：新增 WAL 单测（roundtrip/ack、损坏跳过），cargo check -p uenv-worker 与 cargo test -p uenv-worker wal:: 通过。
- **清单同步**：更新 [worker-pool-mvp-checklist.md](./worker-pool-mvp-checklist.md) 的 M8 勾选状态并追加“M8 现状总结（2026-05-27）”。
## 2026-05-26 23:59

- **M5（Episode 执行引擎，单轮 GSM8K）**：实现 `EpisodeExecutor` 主流程，接通 `PluginHost` 进程级实例执行链路（`spawn -> reset -> step -> close`），并在 Worker `DispatchEpisode` 中替换 M2 占位执行逻辑。
- **模型回调（Mock）**：新增 `ModelClient` 规则，优先读取 `reward_config.target` 生成 action，支持从 payload 兜底提取 `answer`，不依赖真实 vLLM。
- **结果完整性**：`EpisodeResult` 现包含非空 `trajectory` 与 `summary`；新增 `trajectory_checksum`（SHA-256）并设置 `integrity_verified=true`。
- **流式回报**：`DispatchEpisode` 至少发送 1 次 `StreamReport`（`phase=step_complete`，带 `last_step`），并保留后续 `ReportResult` 上报路径与幂等键逻辑。
- **并发与指标**：新增 `Semaphore` 并发门控以尊重 `UENV_MAX_CONCURRENT`；新增 `MetricsExporter` 最小指标聚合与 Prometheus 文本渲染接口（`episode_total`、`episode_duration_ms`、`env_step_duration_ms`、`model_callback_duration_ms`、`active_episode_count`、`heartbeat_lag_ms`）。
- **测试与构建**：新增 `uenv-worker/tests/m5_episode_executor.rs`（Unix 平台）用于 GSM8K fixture 对照断言 reward/status；`cargo check -p uenv-worker` 通过；当前 Windows 环境下该测试因 `cfg(unix)` 约束显示 `0 tests`。
- **清单同步**：更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 的 M5 任务勾选状态并追加“M5 现状总结”。

## 2026-05-26 23:58

- **M6（预热池，固定容量）**：完成 `WarmupPool` 实现，按 `env_type` 维护池队列并落地 `Creating/Warm/Active/Idle/Cooling/Evicting/Destroyed` 状态机。
- **运行时接入**：`WorkerRuntime` 启动阶段新增预热逻辑，按 `pool.warmup_size`（`UENV_WARMUP_POOL_SIZE`）预创建实例并交由执行链路复用。
- **执行链路改造**：`EpisodeExecutor` 从每次 `spawn+close` 改为 `acquire -> reset -> step -> release`，出池前做健康检查，归还时执行 cleanup 并保留进程。
- **回收与补池**：支持 `max_episode_count`、`max_idle_time`、`cool_timeout` 回收；`health_check/reset` 失败实例会销毁并补池，覆盖插件崩溃恢复语义。
- **复用安全与观测**：新增 active 占用检查以避免 double allocation；`dispatch` 日志新增 `warmup_hit` 字段；指标新增 `uenv_warmup_pool_hit_total`、`uenv_warmup_pool_miss_total`、`uenv_instance_pool_size{status}`。
- **测试与验证**：新增 `uenv-worker/tests/m6_warmup_pool.rs`（Unix 平台）覆盖 allocate→release→reallocate；`cargo check -p uenv-worker` 通过；Windows 下 `m5/m6` 两个 Unix 条件测试均为 `0 tests`。
- **清单同步**：更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 的 M6 任务勾选与“M6 现状总结”。

## 2026-05-26 23:55

- **M3（配置与 Linux 日志，ADR-001/ADR-002）**：完成 `uenv-worker` 全局 CLI 参数（`--config` / `--log-level` / `--log-file`）与 `serve|version|health` 子命令对齐。
- **配置加载**：落地 `config` 模块，支持 YAML/JSON 按扩展名解析，支持默认查找序与优先级 `CLI > env > 文件 > 默认`，并补齐 `UENV_*` ↔ 配置字段映射覆盖。
- **日志约束**：落地文本 `.log` 文件写入（默认 `/var/log/uenv/worker.log`），新增单行日志字段校验工具；`UENV_LOG_FORMAT=json` 检测后显式忽略并告警。
- **运行时可观测**：补充 Worker 启停、注册、心跳、Dispatch、ReportResult 的 INFO 事件，统一携带 `trace_id`、`episode_id`、`worker_id` 关键字段。
- **测试**：新增 `config` 映射单测、日志字段解析单测、`m3_serve_with_yaml_config_starts` 集成测试；`cargo test -p uenv-worker` 全量通过。
- **清单**：同步更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md)，M3 全部任务项已勾选并新增 M3 现状总结。

## 2026-05-26 19:23

- **日志缺口补齐（M7 前）**：`EpisodeExecutor` 新增分阶段日志（`acquire/reset/model_callback/step/release`），失败路径统一输出 `error_code` 与 `phase`，补足执行面故障定位信息。
- **控制面日志一致性**：`DispatchEpisode` 路径改为使用真实注册 `worker_id`，并新增 `phase=dispatch_received/dispatch_completed` 与 `warmup_hit` 字段，便于与 Scheduler 侧日志对齐。
- **可观测端点落地**：`uenv-worker` 新增 HTTP 观测服务，默认 `0.0.0.0:19090` 暴露 `GET /metrics` 与 `GET /health`。
- **M6 指标可抓取**：`/metrics` 已暴露 `uenv_warmup_pool_hit_total`、`uenv_warmup_pool_miss_total`、`uenv_instance_pool_size{status}` 等池化指标，满足联调期 scrape 需求。
- **M7 配置切换补充**：新增 `observability.metrics_listen` / `observability.health_listen`（环境变量 `UENV_METRICS_LISTEN` / `UENV_HEALTH_LISTEN`），并同步更新 `config/uenv-worker.{yaml,json}` 与 `uenv-worker/README.md` 联调说明。
- **清单同步**：在 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 的 M7 节新增“Worker 侧联调前补充（2026-05-26）”并勾选完成。

## 2026-05-26 18:25

- **M4（GSM8K 环境插件 + PluginHost）**：完成 `plugins/gsm8k/manifest.yaml` 字段对齐（`version`、`supported_backends: [process]`、`ipc: proto-uds`），并将 `run.sh` 改为可执行入口转发脚本。
- **PluginHost/ProcessBackend**：实现 `PluginHost` 扫描 `UENV_PLUGIN_DIR`、按 `env_type` 索引 manifest、`spawn` 返回 `instance_id + PID + UDS`；实现 `ProcessBackend::create` 启动单插件子进程（1 进程 = 1 instance）。
- **L2 协议调用链**：补齐 `plugin/arpc` 的 `connect_uds`、`reset`、`step`、`close`、`health_check`；`uenv-worker/src/proto.rs` 新增 `plugin::v1` 代码接入，保持 L1/L2 协议分层。
- **GSM8K 子进程插件**：新增 `uenv-worker/src/bin/uenv-gsm8k-plugin.rs`，通过 Protobuf over UDS 实现 `reset/step/close/health_check` 最小语义（固定题目，答案匹配奖励 1.0）。
- **崩溃语义与验证**：`PluginHost` 增加子进程退出监听与实例状态管理；新增 `uenv-worker/tests/m4_plugin_host_process.rs` 覆盖 reset/step/close 与 kill 后不可复用路径（Unix 启用）。
- **验证**：`cargo check -p uenv-worker` 通过；`cargo test -p uenv-worker --test m4_plugin_host_process` 在当前 Windows 环境执行结果为 `0 tests`（`cfg(unix)` 平台约束）。
- **清单**：同步更新 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md)，M4 任务项全部勾选并新增 M4 现状总结。

## 2026-05-26 17:20

- **M2（Worker 运行时骨架 + 对接 Mock）**：实现 `WorkerRuntime` 可运行主流程，`uenv-worker serve` 启动 gRPC Server（`DispatchEpisode` / `HealthCheck`）并并行建立 ControlPlane 注册与心跳。
- **控制面客户端**：落地 `ControlPlaneClient` 的 `register` / `heartbeat_loop` / `report_result`；注册上报 `endpoint`，保持与 `uenv.scheduler.v1.ControlPlaneService` 契约一致，未引入 `subscribe_dispatch`。
- **Dispatch 占位执行语义**：`DispatchEpisode` 增加 `dispatch_lease_id`、`lease_expire_at` 校验；支持 duplicate dispatch 幂等返回与 `lease_conflict` 拒绝；空执行路径构造最小 `EpisodeResult` 并主动 `ReportResult`。
- **集成测试**：新增 `uenv-worker/tests/m2_runtime_with_mock.rs`，覆盖 Mock Scheduler 主动 Dispatch → Worker 回报链路，并通过 `idempotency_key` 重提验证 `ReportResult` 幂等。
- **依赖与协议接线**：`uenv-worker` 对齐 `tonic/prost 0.14`，补充 `tonic-prost`、`prost-types`、`tokio-stream`；新增 `src/proto.rs` 接入 L1 `uenv/v1`、`scheduler/v1`、`worker/v1` 生成代码。
- **验证**：`cargo check -p uenv-worker` 通过；`cargo test -p uenv-worker --test m2_runtime_with_mock` 通过（1 passed）。
- **清单**：同步将 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 的 M2 全部任务项勾选完成，并更新文档“最后更新”日期为 2026-05-26。

## 2026-05-26 00:32

- 完成 M1.6（测试与工具）：新增 `uenv-mock-scheduler/tests/m1_contract_chaos_tests.rs`，内置最小 Worker stub，覆盖 Register→Heartbeat→Dispatch→Report 集成链路；新增 `scripts/gen-gsm8k-fixture.sh`；补充 `grpcurl` 手动探测示例文档。
- 完成 M1.7（协议混沌测试，M1 子集）：自动化覆盖 `duplicate_dispatch`、`unsupported_env_type`、`capacity_full`、`heartbeat_timeout`、`report_result_retry`、`partial_stream_interruption`、`lease_expired`、`lease_superseded`、`server_epoch` 变化等场景（其中租约场景 >=2）。
- 测试验证通过：`cargo test -p uenv-mock-scheduler --test m1_contract_chaos_tests`（7 passed, 0 failed）。
- `worker-pool-mvp-checklist.md` 已更新：M1.6、M1.7 全部勾选完成，M1 退出标准第 6 条更新为“至少 7/10 混沌场景自动化覆盖”。
- 本次写入 `Docs/更新日志.md` 使用显式 UTF-8（无 BOM）方式，遵循文档编码注意事项，避免默认编码造成中文损坏。

## 2026-05-25 23:45

- **文档**：修复 [`更新日志.md`](./更新日志.md) 全文中文变为 `?` 的编码损坏；自未损坏正文整文件恢复，并按时间倒序重排条目。
- **文档**：在文首「文档编码注意事项」新增第 7 条，记录本次全文件 `?` 化事故的现象、根因、处置与预防；新增 `scripts/restore_update_log.py` 供 UTF-8 安全恢复。

## 2026-05-25 23:30

- **文档**：在 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md)、本文件开头补充 **PowerShell / UTF-8 编码维护要点**；修复 checklist 乱码（以 `worker-pool-mvp-checklist_1.md` 为参照合并 v1.3 内容）。
- **根因记录**：高风险操作为未显式 `-Encoding` 的 `Set-Content` / `Add-Content`；下文 `23:07` 条目曾因同类问题乱码，现一并恢复为可读中文。

## 2026-05-25 23:28

- **M1.4（Mock 可观测与日志）**：`uenv-mock-scheduler` 增加文件日志落盘，默认路径 `/var/log/uenv/mock-scheduler.log`（纯文本 `.log`）。
- **配置**：支持日志路径覆盖：CLI `--log-file` 与环境变量 `UENV_LOG_FILE`（环境变量优先）。
- **日志事件**：补齐关键 INFO 事件：`register`、`heartbeat`、`dispatch`、`report_result`；其中 dispatch 日志包含 `worker_id`、`episode_id`、`env_type`、`trace_id`（用 `correlation_id` 映射）。
- **文档**：更新 `uenv-mock-scheduler/README.md`，补充 `tail -f /var/log/uenv/mock-scheduler.log` 观测示例。
- **验证**：`cargo check -p uenv-mock-scheduler` 通过。
- **清单**：同步将 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 中 M1.4 全部子项标记为已完成。

## 2026-05-25 23:18

- **M1.3（Fixture 与任务队列）**：新增 `fixtures/gsm8k/episode_001.pb`、`expected_result_001.pb` 与可读 `episode_001.textproto`，并补充目录说明文档。
- **工具**：新增 fixture 生成器 `uenv-mock-scheduler/examples/gen_gsm8k_fixture.rs`，可一键再生 `.pb` 样例。
- **调度逻辑**：Mock Scheduler 改为「启动加载 fixture 入队 + FIFO 主动下发」；队列为空时输出 WARN 并跳过下发（不阻塞）。
- **租约**：每次 `DispatchEpisode` 下发前生成新的 `dispatch_lease_id`（`lease-<seq>`），用于 failover/租约场景验证。
- **验证**：`cargo check -p uenv-mock-scheduler` 通过。
- **清单**：同步将 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 中 M1.3 全部子项标记为已完成。

## 2026-05-25 23:07

- **M1.2（Mock Scheduler 服务）**：`serve` 从占位改为可运行服务，默认监听 `UENV_MOCK_LISTEN`（默认 `0.0.0.0:50051`）。
- **Mock ControlPlane gRPC**：`RegisterWorker`（记录 endpoint、合成 worker_id）、`WorkerHeartbeat`（双向流 Ack + `next_heartbeat_interval_ms` + `server_epoch`）、`ReportResult`（按 `idempotency_key` 幂等去重）、`ListWorkers`（内存注册表查询）。
- **主动调度**：从 `--fixture-dir` 读取 `.pb` `EpisodeRequest` 队列，向已注册 Worker `endpoint` 发起 `DispatchEpisode`，消费 `StreamReport`。
- **Proto 与依赖**：新增 `uenv-mock-scheduler` proto 包装模块；依赖对齐 `tonic 0.14` / `prost 0.14`，启用 `tonic-prost`、`prost-types`。
- **验证**：`cargo check -p uenv-mock-scheduler` 通过。
- **清单**：同步将 [`worker-pool-mvp-checklist.md`](./worker-pool-mvp-checklist.md) 中 M1.2 全部任务标记为已完成。

## 2026-05-25 21:40

依据 [worker-pool-pre-mvp-architecture-adjustment.md](./worker-pool-pre-mvp-architecture-adjustment.md) 完成 **MVP 前置架构调整（阶段 0）**；Gate 8 项已全部满足，可进入 [worker-pool-mvp-checklist.md](./worker-pool-mvp-checklist.md) M1。

- **Crate**：新建 `uenv-mock-scheduler/`（`serve` / `version` CLI 占位）；根目录新增 `Cargo.toml` workspace（`uenv-worker`、`uenv-mock-scheduler`）；`uenv-server`、`uenv-hub` 各自 `[workspace]` 独立构建。
- **uenv-worker 模块树**：扁平 `src/` 重组为 design §13 结构（`cli/`、`config/`、`control_plane/`、`grpc_server/`、`episode/`、`pool/`、`plugin/`、`backend/`、`wal/`、`logging/`、`runtime.rs` 等）；删除旧 `executor.rs`、`inference.rs`、`warmpool.rs`、`grpc.rs`。
- **命名对齐**：`WarmPool` → `WarmupPool`；`InferenceClient` → `ModelClient`；Worker 状态机 → `Created / Ready / Busy / Draining / Terminated`。
- **Proto L1**：共享定义迁至 `proto/uenv/v1/`（`common.proto`、`episode.proto` 含租约字段、`wal.proto`、`scheduler.proto`）；Worker 侧 `uenv-worker/proto/worker_service.proto`（`DispatchEpisode` 返回 `stream StreamReport`、`HealthCheck`）；删除旧 `worker.proto` 及 `uenv-server/proto/uenv/v1/` 重复拷贝。
- **Proto L2**：新建 `plugin_proto/uenv/plugin/v1/plugin.proto`（reset / step / close / health_check）；新增 `proto/README.md` 说明 L1/L2 边界。
- **顶层目录**：`plugins/gsm8k/manifest.yaml`、`fixtures/gsm8k/` 占位；`config/uenv-worker.yaml` / `uenv-worker.json`、`config/uenv-mock-scheduler.yaml`；`config/worker.example.toml` 标记 deprecated。
- **uenv-server**：`wal`、`backend` 模块标记 deprecated，职责归 Worker 侧；README 标明 M7 前 Worker Pool MVP 不阻塞完整 server。
- **构建**：`Makefile`、`scripts/proto-gen.sh` 对齐 canonical proto 路径，改用 `--prost_out` / `--tonic_out`；补充 `proto-mock-scheduler`、`proto-plugin` 目标。
- **文档与 CLI**：根 `README.md`、`uenv-worker/README.md`、`uenv-bridge/README.md` 统一 `serve` 子命令与 Layer 2 控制面叙述；`uenv-worker serve|version|health`、`uenv-mock-scheduler serve|version` 占位入口。
- **清单状态**：`worker-pool-pre-mvp-architecture-adjustment.md` §4 / §5 / §8 标记完成。
