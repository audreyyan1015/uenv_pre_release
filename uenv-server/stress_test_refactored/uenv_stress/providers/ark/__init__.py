"""Ark 模型服务轨迹采集工具包。

这个目录包含 Ark 代理和预检脚本，用于 Gate3 或轨迹采集阶段通过真实模型生成可冻结的响应证据。它不直接参与稳定性 replay 的调度，只负责让采集阶段的模型调用可检查、可限制在本机回环地址上。

实现逻辑是：preflight 检查代理健康和最小调用是否可用；proxy 读取 OpenHands LLM 配置，把请求转发到 Ark，并把 token 元数据补回响应，供后续轨迹文件记录。"""
