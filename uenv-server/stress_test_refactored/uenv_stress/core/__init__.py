"""压测公共能力包。

这个目录集中放置多个压测入口都会复用的基础能力，包括 Episode 构造、结果格式、worker fleet 描述、远程进程管理、生产端口保护、运行配置解析和指标聚合。这样可以避免每个压测脚本重复实现同一套协议和安全检查。

实现逻辑是：上层 cli/scale/stability 脚本只描述本次要跑什么任务；core 模块负责提供可复用函数，统一处理 JSON payload、SSH 连接、端口监听检查、受保护进程快照、子进程启动停止、CSV/JSON 结果字段和统计计算。"""
